function yPredict = classifyMLPModel(trainedModel , inputPatterns)
    % this function return class label from input pattern.
    % trainedModel is model JST that already trained
    % inputPattern is data testing
    
    yPredict = [];
    nRow = trainedModel.
    for i = 1 : nRow
       
        
    end
end